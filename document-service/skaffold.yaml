apiVersion: skaffold/v2beta4
kind: Config
metadata:
  name: documents-service
build:
  artifacts:
  - image: dthibau/documents-service
    context: .
    jib: 
      args:
      - -DskipTests
deploy:
  kubectl:
    manifests:
    - k8s/deployment.yaml
